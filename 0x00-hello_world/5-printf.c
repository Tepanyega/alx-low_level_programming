#include <stdio.h>
/**
 * main - A progarm that print a line using printf function
 * Return: o (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
